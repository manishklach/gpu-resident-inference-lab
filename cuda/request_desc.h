/**
 * request_desc.h - Backward-compatible wrapper.
 *
 * This file is kept at the old path for projects that include
 * "request_desc.h" directly. The canonical header now lives at
 * include/request_desc.h.
 *
 * Include the new location to pick up the full lifecycle states,
 * draft tokens, and updated descriptor layout.
 */

#pragma once
#include "include/request_desc.h"
